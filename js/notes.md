/js
  /data
    steps-data.js          // STEP_ORDER + STEP_DATA (shared)
  /utils
    escapeHtml.js          // shared
  lifecycle-mobile.js      // all mobile logic (render + events + state inside)
  lifecycle-desktop.js     // all desktop logic (render + events + state inside)
  main.js                  // decides which one to init